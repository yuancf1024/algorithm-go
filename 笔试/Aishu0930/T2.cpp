/*
SQL 查找所有连续且仅出现2次的数字

编写一个SQL查询，查找所有连续且仅出现两次的数字。
返回的结果表中的数据可以按任意顺序排列。

示例1：
输入：
drop table if exists Logs;
create table Logs (id int, num int);
insert into Logs values(1,1), (2,1), (3,1), (4,2), (5,2), (6,1), (7,2)

输出：
consecutiveNums
2



*/